name: lint
on:
  push:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    container:
      image: ros:foxy

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run lint
        run:
          make lint
